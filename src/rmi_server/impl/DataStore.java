package rmi_server.impl;

// kept for backward compatibility / optional use.
// Main code uses MySQL DAOs, but keep a lightweight in-memory map if needed.
public class DataStore {
    // intentionally empty — MySQL DAO is used in implementations.
}
